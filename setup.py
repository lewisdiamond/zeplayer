from setuptools import setup, find_packages

setup(
    name='zePlayer',
    version='0.0.1',

    description='Ze Player!',

    url='https://github.com/lewisdiamond/zeplayer',

    author='Lewis Diamond',

    packages=find_packages(exclude=['contrib', 'docs', 'tests'])
)
